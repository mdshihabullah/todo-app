# Issue tracker

This repo uses **GitHub Issues** for tracking work. The `gh` CLI can be used to create and manage issues.

**PRs as a request surface**: off (default). External PRs are not surfaced in the triage queue unless explicitly flipped.